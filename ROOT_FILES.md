# Root compatibility files

The root-level `script.js`, `style.css`, `dictionary-engine.js`, and `pdf-generator.js` are compatibility shims/entry points only.

**Canonical code lives under `src/`.**

Developers should edit `app.js`, `src/engines/...`, `src/services/...`, and `src/data/...` rather than the root shims.
